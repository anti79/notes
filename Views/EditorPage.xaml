<Page
    x:Class="notes.Views.EditorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:notes.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:v="using:notes.Views" xmlns:v1="using:notes"
	xmlns:vm="using:notes.ViewModel" xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
	mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

	<Grid>
		<Border Background="AntiqueWhite">
			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition Height="11*"></RowDefinition>
					<RowDefinition Height="89*"></RowDefinition>
				</Grid.RowDefinitions>
				<Grid>
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="62*"></ColumnDefinition>
						<ColumnDefinition Width="13*"></ColumnDefinition>
					</Grid.ColumnDefinitions>
					<TextBox Text="{Binding Title, Mode=TwoWay}" PlaceholderText="Title" FontSize="50"></TextBox>
					<Button Grid.Column="1" x:Name="saveBtn" Command="{Binding ExitCommand}" Width="250" >Save</Button>
				</Grid>
				<RichEditBox Grid.Row="1" x:Name="textEditorBox" >

				</RichEditBox>
			</Grid>

		</Border>
	</Grid>
</Page>
